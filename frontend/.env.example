# Example Vite env for dev to point API calls at a specific backend
# Copy this to frontend/.env when running the dev server locally and restart Vite.
VITE_API_BASE=https://capi-production-7bf3.up.railway.app
